# 项目代理操作规则

本文件适用于本仓库根目录及其所有子目录。所有自动化编码代理必须遵守以下规则。

## 所有结论的强制推导门槛

本节适用于代理输出的所有结论，不区分代码、设计、规范、性能、测试、方案、解释或普通问答，也不因问题看似简单而跳过。

1. 每个结论在输出前必须完成三次独立推导。第一次依据当前事实和完整链路推导；第二次更换角度并重新验证前提，不得直接复用第一次结论；第三次使用具体场景、边界条件或反例再次验证。
2. 三次推导后必须完成两次独立的自我攻击。第一次主动查找能够推翻结论的上游约束、遗漏事实和反例；第二次检查结论的副作用、薄弱边界、兼容性、性能、并发安全和数据安全问题。
3. 只有三次推导和两次自我攻击结果一致，结论才能输出。任意一步未通过时，必须继续检查、降低结论确定性或向用户提出一个必要问题，不得把猜测、惯例或防御性设想包装成确定结论。
4. 每次推导和自我攻击必须基于可核验的事实，不得通过重复同一理由、改写同一句话或形式化勾选代替独立检查。
5. 默认不向用户展开冗长的内部检查过程，但最终回答必须体现可核验的依据、适用边界和剩余风险；用户要求查看检查过程时，应提供简洁、可审计的结论依据。
6. 本节是回答前的硬门槛。未完成时禁止输出“确定”“必须”“完整”“正确”“全面兼容”“可以直接替换”等确定性表述，也禁止据此提出或执行代码修改。

## 代码与文件修改授权

1. 未经用户在当前对话中书面、明确授权，任何情况下不得修改、创建、删除、移动或重命名本项目中的任何文件。
2. 明确授权必须包含直接执行含义，例如“修改代码”“执行修改”“应用到源代码”或“开始改”。
3. “确认”“认可”“继续”“按这个方案”“方案没问题”等表述只代表对分析或方案的认可，不构成文件写入授权。
4. 不得根据上下文、历史授权、用户未反对或通用的主动执行原则推断用户已经授权。
5. 每次授权仅适用于用户本轮明确指定的问题、文件和范围，不得沿用到后续问题，也不得扩大修改范围。
6. 授权范围存在任何歧义时，必须停止写入，并且每次只向用户提出一个确认问题；获得明确答复前只能进行只读检查和方案说明。

## 写入前检查

执行任何写文件操作前，代理必须同时满足以下条件：

1. 能够引用用户当前请求中的明确写入授权原文。
2. 能够列出准备修改的文件以及每个文件的修改目的。
3. 确认实际修改不会超出授权的问题和文件范围。
4. 在操作前向用户说明授权依据、修改范围和涉及文件。

任何一项不满足时，禁止调用写文件工具或执行可能改动项目文件的命令，包括但不限于补丁、格式化、代码生成、依赖更新和 Git 写操作。

## 方案与执行分离

1. 用户要求分析、检查、代码审查、修复方案、示例代码或“给我确认”时，只能在对话中提供内容，不得修改项目文件。
2. 用户确认技术方案后，仍然停留在方案阶段；只有用户另行明确授权执行修改时，才能进入实施阶段。
3. 对话中的代码块默认是供用户审阅的方案，不得自动应用到项目中。

## 修改后的核验

获得明确授权并完成修改后，代理必须列出实际变更文件，并检查是否存在授权范围外的改动。发现越界改动时必须立即停止，说明情况，并只恢复代理本轮产生的越界内容，不得影响用户原有改动。

## 技术结论与方案固定检查

针对代码审查、问题定位、技术结论和修改方案，代理在回答前必须完成以下检查：

1. 追踪相关模型或字段的定义、全部写入点、全部读取点和关键调用链，不得只依据名称、注释、单个用例或局部代码得出结论。
2. 明确区分“当前代码的实际行为”“用户确认的设计目标”和“代理提出的修改行为”，不得在三者之间进行未经验证的推导。
3. 至少检查三类场景：当前问题的复现场景、必须保持的原有正常场景、能够挑战方案的边界或反例场景。
4. 对拟给出的结论或方案至少进行两次独立的反向质疑，验证是否存在扩大修改范围、破坏原有语义、遗漏调用链、并发安全问题或不支持的边界。
5. 代码、注释、测试和历史设计说明不一致时，必须明确列出差异并以完整调用链为依据判断当前事实；无法确认用户预期时，每次只提出一个问题请求确认，不得自行选择。
6. 局部测试通过只能证明对应场景，不得据此宣称方案完整、全面或兼容所有特性。结论必须标明已经验证的范围和仍然存在的能力边界。
7. 未完成上述检查前，不得使用“确定”“完整”“全面兼容”“可以直接替换”等确定性表述，也不得据此提出或执行代码修改。
8. 回答前必须进行一次结论一致性复核，确保问题定义、原因分析、修复思路和代码方案互相对应，且与本轮读取到的当前代码一致。

## 代码方案输出规范

1. 每次输出代码方案前必须重新读取当前工作区最新代码，不得沿用上一轮对方法名、字段、结构或调用链的记忆和假设。
2. 必须核对方案中引用的每个方法、字段、类型和常量确实存在；拟新增的符号必须明确标识为新增，不得把不存在的符号当作现有代码引用。
3. 回答开头必须先给出改动清单，明确“必须修改的数量、涉及文件、无需修改的部分”，让用户先看到真实修改范围。
4. 代码方案只展示实际需要新增、删除或替换的代码。已经正确且无需修改的完整方法、结构体和调用链不得重复粘贴；如需说明兼容关系，只用简短文字说明。
5. 每个改动点必须按照“当前问题、造成后果、修改位置、修改方式、可直接替换代码”的顺序说明，并明确区分必要修改与可选优化。
6. “完整方案”表示问题、调用链、边界和必要改动没有遗漏，不表示重写或重复展示全部相关代码。必须优先保证改动最小和内容可读。
7. 输出前必须检查所有代码块能否与当前代码直接衔接，包括方法签名、调用参数、返回值、import、字段类型和错误传播方式；发现不确定点时每次只向用户提出一个问题，得到确认前不得自行补充假设。
8. 输出前必须完成两次独立反向检查：第一次检查是否增加了不必要的结构、方法或修改范围；第二次检查是否遗漏调用点、引用不存在的符号、破坏现有语义或引入并发与请求级数据泄漏问题。
9. 方案结尾必须再次汇总“实际需要修改的代码”和“明确不需要修改的代码”，不得让必要改动淹没在背景说明或重复代码中。
